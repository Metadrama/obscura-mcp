{
  "$schema": "https://glama.ai/mcp/schemas/server.json",
  "maintainers": ["metadrama"],
  "sourceUrl": "https://github.com/Metadrama/obscura-mcp.git",
  "source": "github",
  "version": "0.1.4-1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "obscura-mcp",
      "version": "0.1.4-1",
      "transport": {
        "type": "stdio"
      }
    }
  ],
  "tools": [
    {
      "name": "browse_page",
      "description": "Fetch a page and extract content as text, markdown, html, links, or cookies. Optionally evaluate JavaScript."
    },
    {
      "name": "browse_interact",
      "description": "Click an element or type text into a page."
    },
    {
      "name": "browse_session",
      "description": "Create and manage persistent browser sessions for multi-step flows."
    },
    {
      "name": "browse_scrape",
      "description": "Scrape multiple URLs in parallel using worker processes. Returns structured JSON with per-URL results."
    }
  ]
}
